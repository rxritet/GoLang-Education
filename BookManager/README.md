# 📚 Books REST API

Простой REST API на чистом Go для управления коллекцией книг. Без внешних зависимостей — только стандартная библиотека. Включает веб-интерфейс для работы с API прямо из браузера.

## Возможности

- Полный CRUD для книг (создание, чтение, обновление, удаление)
- Потокобезопасное in-memory хранилище (`sync.RWMutex`)
- CORS включён — фронтенд и API могут работать на разных портах
- Встроенный веб-интерфейс (`index.html`) без дополнительных инструментов
- 3 книги предзагружены для быстрого старта

## Стек

- **Go** 1.25
- `net/http` — HTTP-сервер и маршрутизация
- `encoding/json` — сериализация
- `sync` — потокобезопасность хранилища

## Структура проекта

```
thirdproject/
├── main.go           # Точка входа, настройка сервера и маршрутов
├── go.mod            # Модуль Go
├── models/
│   └── models.go     # Структура Book и in-memory Store
├── handlers/
│   └── handlers.go   # HTTP-обработчики и маршрутизатор
└── static/
    └── index.html    # Веб-интерфейс
```

## Запуск

```bash
git clone <url>
cd thirdproject
go run .
```

Сервер поднимется на `http://localhost:8080`.  
Веб-интерфейс доступен по адресу `http://localhost:8080`.

## API

Базовый URL: `http://localhost:8080`

| Метод    | Endpoint          | Описание               |
|----------|-------------------|------------------------|
| `GET`    | `/api/books`      | Список всех книг       |
| `GET`    | `/api/books/{id}` | Книга по ID            |
| `POST`   | `/api/books`      | Создать книгу          |
| `PUT`    | `/api/books/{id}` | Обновить книгу         |
| `DELETE` | `/api/books/{id}` | Удалить книгу          |

### Модель Book

```json
{
  "id": 1,
  "title": "The Go Programming Language",
  "author": "Alan A. A. Donovan",
  "year": 2015
}
```

> Поля `title` и `author` — обязательны при создании и обновлении.

### Примеры запросов

**Получить все книги**
```bash
curl http://localhost:8080/api/books
```

**Создать книгу**
```bash
curl -X POST http://localhost:8080/api/books \
  -H "Content-Type: application/json" \
  -d '{"title":"Clean Architecture","author":"Robert C. Martin","year":2017}'
```

**Обновить книгу**
```bash
curl -X PUT http://localhost:8080/api/books/1 \
  -H "Content-Type: application/json" \
  -d '{"title":"New Title","author":"New Author","year":2024}'
```

**Удалить книгу**
```bash
curl -X DELETE http://localhost:8080/api/books/1
```

## Заметки

- Данные хранятся **в памяти** — после перезапуска сервера сбрасываются
- Для продакшна нужно заменить `Store` на реальную БД (PostgreSQL, SQLite)
